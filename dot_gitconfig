[user]
	name = Gabriel Gellner
	email = gabriel.gellner@proton.me

# Machine-local extras (credential helpers, work identity overrides, GPG keys, etc.) — not in chezmoi
[include]
	path = ~/.gitconfig.local

[init]
	defaultBranch = main

[pull]
	rebase = false

[push]
	default = simple
	autoSetupRemote = true
