# timout for commands, defaults to 500
# command_timeout = 1000

[container]
disabled = true

# Stop showing the global python on PATH
[python]
disabled = true

# Show the uv environment python version when in a uv project
[custom.uv_python]
when = "test -f pyproject.toml"
command = "test -f .python-version && cat .python-version || uv run python --version 2>/dev/null | cut -d' ' -f2"
format = "via [🐍 $output]($style) "
style = "green"
# [custom.uv_python]
# when = "test -f .python-version || test -f pyproject.toml"
# command = "uv run python --version 2>/dev/null | cut -d' ' -f2"
# format = "via [🐍 $output]($style) "
# style = "green"

[character]
success_symbol = "[❯](bold green)"
error_symbol = "[❯](bold red)"
vimcmd_symbol = "[❮](bold green)"
vimcmd_replace_one_symbol = "[❮](purple)"
vimcmd_replace_symbol = "[❮](purple)"
vimcmd_visual_symbol = "[❮](yellow)"
