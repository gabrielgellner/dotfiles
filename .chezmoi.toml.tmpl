[git]
    # `chezmoi re-add`/`add`/`apply` auto-commit to the source repo and push to
    # origin, so capturing a live edit (e.g. an nvim config change) is one step.
    autoCommit = true
    autoPush = true
